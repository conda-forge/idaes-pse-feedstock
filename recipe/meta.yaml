{% set name = "idaes-pse" %}
{% set version = "2.10.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/IDAES/{{ name }}/archive/{{ version }}.tar.gz
  sha256: e98c4805cbdc44f416a9bb43c2e08d93abf6e97d2c70910d08f8534a7e146955

build:
  noarch: python
  number: 0
  entry_points:
    - idaes = idaes.commands.base:command_base
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --no-cache-dir --ignore-installed"

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - click >=8
    - matplotlib-base
    - networkx
    - numpy >=1.17
    - pandas
    - pint
    - pip
    - pyomo >=6.9.1
    - pytest
    - pyyaml
    - scipy
    - sympy

test:
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - idaes
    - idaes.core
    - idaes.core.base
  commands:
    - pip check
    - idaes --version
    - idaes --help

about:
  home: "https://idaes.org"
  license: BSD-3-Clause-LBNL
  license_family: BSD
  license_file: LICENSE.md
  summary: "IDAES Process Systems Engineering Framework"
  doc_url: "https://idaes-pse.readthedocs.io"
  dev_url: "https://github.com/IDAES/idaes-pse"

extra:
  recipe-maintainers:
    - lbianchi-lbl
